<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>@ViewBag.Title</title>
    @Styles.Render("~/Content/stylesheet.css")
</head>
    <body>
        <div id="top_bar_black"> <div class="nav_button">@Html.ActionLink("Main","Index","Home",null,new { id = "menu" })</div>
	  <div class="nav_button">@Html.ActionLink("Directory","ShowData","Directory",null,new { id = "menu" }) </div>
 </div>
        <div id="content_container">
            <div id="header"> <div class="header_content_mainline"> Telephone directory of Kharkov </div>  
                @RenderSection("data",required:false)  
                @RenderSection("main",required:false)   
                @RenderSection("edition",required:false)   
                @RenderSection("adding",required:false)  
                @RenderSection("delete",required:false) 
            </div>  
</div> 
</body>
</html>